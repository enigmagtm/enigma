{
  "name": "@enigmagtm/cli",
  "version": "1.1.1",
  "description": "Enigma command line tool",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "install-pkg": "npm i --no-save",
    "build": "rm -rf dist && tsc -p tsconfig.json && cp {README.md,package.json,LICENSE.md} dist",
    "link": "npm run build && npm link",
    "unlink": "npm unlink",
    "publish-pkg": "npm run build && cd dist && npm publish --access public"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/enigmagtm/enigma.git",
    "directory": "packages/cli"
  },
  "keywords": [
    "ngm",
    "enigma",
    "cli"
  ],
  "license": "MIT",
  "bin": {
    "ngm": "./dist/src/cli.js"
  },
  "dependencies": {
    "knex": "^0.95.14",
    "prompt-sync": "^4.2.0"
  },
  "devDependencies": {
    "@types/knex": "^0.16.1",
    "@types/prompt-sync": "^4.1.1"
  }
}
